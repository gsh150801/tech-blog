{
  "name": "shiji-blog",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:drafts": "INCLUDE_DRAFTS=1 astro build && INCLUDE_DRAFTS=1 pagefind --site dist",
    "preview": "astro preview",
    "preview:drafts": "INCLUDE_DRAFTS=1 astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.0.0",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
